//NOTE: the cwd should contain the python files for the python embedding to work
#include "getTracks.h"

#include <iostream> 
#include <vector> 
#include <unistd.h> 
#include <string.h> 
#include <sys/socket.h> 
#include <sys/types.h> 
#include <sys/un.h>

int hypedExec(char *str)
{
  return 0;
}
